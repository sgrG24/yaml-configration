format_version: 9
environments:
  testing:
    environment_variables:
      DEPLOYMENT: testing
    secure_variables:
      ENV_PASSWORD: "s&Du#@$xsSa"
    pipeline:
      - pipe1
pipeline:
  pipe1:
    group: sample
    materials:
      mygit:
        git: https://github.com/sgrG24/newCPPproject.git
        branch: master
    stages:
      build:
        jobs:
          tasks:
           - exec: 
             command: ./a.out
